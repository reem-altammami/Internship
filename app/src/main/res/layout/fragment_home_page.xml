<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
<variable
    name="companyViewModel"
    type="com.reem.internship.model.CompanyViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".HomePageFragment">
<Button
    android:id="@+id/filter_major"
    android:layout_width="204dp"
    android:layout_height="wrap_content"
    android:maxLines="1"
    android:text="@string/major"
    app:icon="@drawable/ic_arrow_drop_down"
    app:iconSize="24dp"
    app:iconTint="@color/black"
    android:textColor="@color/black"
    android:backgroundTint="@color/white"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toStartOf="@id/filter_city"/>
        <Button
            android:id="@+id/filter_city"
            android:layout_width="204dp"
            android:layout_height="wrap_content"
            android:text="@string/city"
            app:icon="@drawable/ic_arrow_drop_down"
            android:maxLines="1"
            app:iconSize="24dp"
            app:iconTint="@color/black"
            android:textColor="@color/black"
            android:backgroundTint="@color/white"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/filter_major"
            app:layout_constraintEnd_toEndOf="parent"/>
        <LinearLayout
            android:padding="4dp"
            android:layout_marginTop="40dp"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/training_recycler_view"
            android:layout_marginTop="40dp"
            app:layout_constraintTop_toBottomOf="@id/filter_major"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:listData="@{companyViewModel.uiState.trainingItemList}"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>